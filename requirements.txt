# VibiKey requirements
#
# Install modes (PEP 508 extras markers):
#   pip install -r requirements.txt                 # runtime only
#   pip install -r requirements.txt --extra onefile # Nuitka[app] (single-file builds)
#   pip install -r requirements.txt --extra all     # Nuitka[all]
#   pip install -r requirements.txt --extra build   # PyInstaller (single-file builds)

# --- Runtime (needed to run run_ui.py) ---
PySide6-Essentials>=6.5
pynput>=1.8

# --- Packaging (onefile is the default build mode) ---
# Nuitka onefile needs the [app] extra (zstandard compression, etc.).
Nuitka[app]>=2.4 ; extra == "onefile"

# `--extra all`: Nuitka with every optional dependency (Nuitka[all]).
Nuitka[all] ; extra == "all"

# PyInstaller onefile backend.
pyinstaller>=6.0 ; extra == "build"
ordered-set>=4.1 ; extra == "onefile"
zstandard>=0.20 ; extra == "onefile"
