# VibiKey requirements
#
# Install modes (PEP 508 extras markers):
#   pip install -r requirements.txt                 # runtime + Nuitka (basic)
#   pip install -r requirements.txt --extra onefile  # + Nuitka[app] for onefile
#   pip install -r requirements.txt --extra all      # + Nuitka[all] (everything)
#   pip install -r requirements.txt --extra build     # + PyInstaller

# --- Runtime (needed to run run_ui.py) ---
PySide6-Essentials>=6.5
pynput>=1.8

# --- Nuitka ---
# Base: Nuitka (basic packaging backend).
Nuitka>=2.4

# `--extra onefile`: Nuitka with onefile-supporting dependencies (Nuitka[app]).
Nuitka[app] ; extra == "onefile"

# `--extra all`: Nuitka with every optional dependency (Nuitka[all]).
Nuitka[all] ; extra == "all"

# `--extra build`: PyInstaller backend (alternative to Nuitka).
pyinstaller>=6.0 ; extra == "build"
