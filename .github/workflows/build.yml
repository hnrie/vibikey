name: Build

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
  release:
    types: [published]

env:
  # Force Node 24 for actions ahead of the spring-2026 Node 20 EOL.
  # Silences the "Node.js 20 is deprecated" warning for actions still
  # shipping a Node 20 runtime (e.g. actions/upload-artifact@v5).
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  windows-pyinstaller:
    name: Windows (PyInstaller)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Create venv and install deps
        shell: pwsh
        run: |
          python -m venv .venv
          ./.venv/Scripts/Activate.ps1
          python -m pip install --upgrade pip
          pip install PySide6-Essentials pynput pyinstaller

      - name: Build CatKey (PyInstaller, onedir)
        shell: pwsh
        run: |
          ./.venv/Scripts/Activate.ps1
          ./build.ps1 -Tool pyinstaller

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: CatKey-Windows-PyInstaller
          path: dist/CatKey
          if-no-files-found: error

  windows-nuitka:
    name: Windows (Nuitka)
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Create venv and install deps
        shell: pwsh
        run: |
          python -m venv .venv
          ./.venv/Scripts/Activate.ps1
          python -m pip install --upgrade pip
          pip install PySide6-Essentials pynput nuitka

      - name: Build CatKey (Nuitka, standalone)
        shell: pwsh
        run: |
          ./.venv/Scripts/Activate.ps1
          ./build.ps1 -Tool nuitka

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: CatKey-Windows-Nuitka
          path: dist/run_ui.dist
          if-no-files-found: error

  linux:
    name: Linux (PyInstaller)
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          sudo apt-get update
          sudo apt-get install -y build-essential \
            libgl1 libegl1 libglib2.0-0 libxkbcommon0 libdbus-1-3 \
            libxcb-xinerama0 libxcb-cursor0 libxcb-icccm4 libxcb-keysyms1 \
            libxcb-image0 libxcb-shm0 libxcb-randr0 libxcb-render-util0 \
            libxcb-xkb1 libxcb1 libfontconfig1 libfreetype6
          pip install PySide6-Essentials pynput pyinstaller

      - name: Build CatKey (PyInstaller)
        run: |
          chmod +x build.sh
          ./build.sh --tool pyinstaller

      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: CatKey-Linux-PyInstaller
          path: dist/CatKey
          if-no-files-found: error

  release:
    name: Release assets
    needs: [windows-pyinstaller, windows-nuitka, linux]
    if: github.event_name == 'release'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/download-artifact@v5
        with:
          path: artifacts
      - name: Publish release assets
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/**/*
